export default function Footer() {
  return (
    <div
      id="Footer"
      style={{ backgroundColor: "white", borderTop: "1px solid" }}
    >
      footer meant for credits to geodesy lib, wikidata and any api that this
      app uses
    </div>
  );
}
